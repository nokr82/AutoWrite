__pycache__/
*.pyc

# PyInstaller 빌드 산출물 (build.spec으로 만든 exe/중간 파일)
build/
dist/

# 로그인 세션(쿠키), 업로드 이미지, 게시 로그, 에러 스크린샷 — 개인 계정 정보라 절대 커밋하지 않음
storage/

# 블로그 이름/갤러리ID 등 개인 설정. 없으면 앱 최초 실행 시 자동으로 템플릿이 생성됨
config.json

# 최초 실행 시 자동으로 내려받는 Chromium 브라우저 (exe 옆 고정 폴더에 저장)
browsers/
